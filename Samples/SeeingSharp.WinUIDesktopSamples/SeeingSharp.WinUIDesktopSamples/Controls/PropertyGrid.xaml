<UserControl
    x:Class="SeeingSharp.WinUIDesktopSamples.Controls.PropertyGrid"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid x:Name="GridMain"
          Background="{ThemeResource GenericAcrylicBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.Resources>
            <Style TargetType="Rectangle">
                <Setter Property="Fill" Value="{ThemeResource SystemBaseMediumColor}" />
            </Style>
        </Grid.Resources>

        <Grid.ChildrenTransitions>
            <ContentThemeTransition HorizontalOffset="-10" VerticalOffset="0" />
        </Grid.ChildrenTransitions>
    </Grid>
</UserControl>